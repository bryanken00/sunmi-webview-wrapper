plugins {
    // Pinned to the AGP/Kotlin pair that Gradle 8.0.x supports. If you open
    // this in a newer Android Studio, let it upgrade all three together.
    id("com.android.application") version "8.1.4" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
